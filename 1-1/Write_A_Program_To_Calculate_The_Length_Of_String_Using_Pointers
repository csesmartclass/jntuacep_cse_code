
 #include<stdio.h>
	#include<conio.h>
	
	void main()
	{
		char s[100];
		char *p;
		int length;
		clrscr();
		printf("Enter a string\n");
		scanf("%s",s);
		for(p=s;*p!='\0';p++);
			length=p-s;
		printf("Length of string is::%d",length);
		getch();
    }

